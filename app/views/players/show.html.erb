<h2><%= @player.name %></h2>
<span><%= link_to 'Edit', edit_player_path(@player) %></span>
<span><%= link_to 'Delete', player_path(@player), :method => :delete, :confirm => "Are you sure you want to delete #{@player.name}?" %></span>
