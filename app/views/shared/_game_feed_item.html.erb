<li>
    <% game_feed_item.teams.each_with_index do |team, index| %>
      
      <%= raw html = team.players.map { |player| link_to(player.display_name_for_result(current_player, game_feed_item), player_game_path(player, @game)) }.join(" and ") %>
            
      <% if index == 0 %> beat <% end %>
    
    <% end %>
</li>
