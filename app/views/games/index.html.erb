<div class="games-header">
  <h1>Games</h1>
  <div><%= link_to "New game", new_game_path, data: { turbo_frame: "new_game" } %></div>
</div>

<%= turbo_frame_tag "new_game", target: "_top" %>

<div id="games">
  <%= render @games %>
</div>